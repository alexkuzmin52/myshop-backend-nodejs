export enum OrderStatusEnum {
  COMPLETED = 'completed',
  CONFIRMED = 'confirmed',
  DELIVERED = 'delivered',
  PAID = 'paid',
  RECEIVED = 'received',
  REGISTERED ='registered',
  RETURNED = 'returned',
  SENT = 'sent',
}
