export enum OrderStatusEnum {
  REGISTERED ='registered',
  CONFIRMED = 'confirmed',
  COMPLETED = 'completed',
  SENT = 'sent',
  DELIVERED = 'delivered',
  RECEIVED = 'received',
  RETURNED = 'returned',
  PAID = 'paid',
}
